
--if OType is MKT, order should not have a price--

bid/ask_qty are local copies, they must be updated inside loop

check if bestBid/Ask() are not empty

need to make sure bestPrice satifies LMT order price on each iteration
